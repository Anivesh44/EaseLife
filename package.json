{
  "name": "easelife",
  "version": "1.0.0",
  "description": "EASELIFE - Connecting people to service providers",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && nodemon index.js",
    "client": "cd client && cross-env HOST=0.0.0.0 npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "service-provider",
    "labor",
    "workers"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "all": "^0.0.0",
    "mongoose": "^9.0.1"
  }
}
